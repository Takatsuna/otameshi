<%= form_for(@fuga) do |f| %>
  <% if @fuga.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fuga.errors.count, "error") %> prohibited this fuga from being saved:</h2>

      <ul>
      <% @fuga.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :hoge %><br>
    <%= f.text_field :hoge %>
  </div>
  <div class="field">
    <%= f.label :hogehge %><br>
    <%= f.text_field :hogehge %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
